// write an index endpoint that sends this message
// 'accessed public endpoint'

// write a show endpoint that sends this message
// 'accessed public/:id endpoint'
// the :id part is the route params that you get from the req object

module.exports = {
  index,
  show
}